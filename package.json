{
  "name": "npm-package",
  "version": "0.0.1",
  "licence": "MIT",
  "author": "Alexandre Dos Reis",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "typecheck": "tsc",
    "ci": "pnpm typecheck && pnpm build",
    "release": "pnpm typecheck && pnpm build && pnpm exec changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.1",
    "tsup": "^6.7.0",
    "typescript": "^5.0.2"
  }
}
